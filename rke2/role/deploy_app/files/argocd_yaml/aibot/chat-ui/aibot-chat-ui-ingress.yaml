apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aibot-chat-ui-ingress
  namespace: vas-aibot
spec:
  rules:
  - host: chat-ui.HOSTIP.nip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: aibot-chat-ui-service
            port:
              number: 8080
  ingressClassName: nginx

